import { ApiImageDirective } from './api-image.directive';

describe('ApiImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ApiImageDirective();
    expect(directive).toBeTruthy();
  });
});
